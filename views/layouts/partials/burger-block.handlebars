<li>
	{{burger_name}}

	<button class="change-eat" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}Burger TIME!{{else}}Not Burger TIME!{{/if}}
	</button>

	{{!-- <button class="delete-cat" data-id="{{this.id}}">DELETE!</button> --}}
	
</li>
